
source 'https://rubygems.org'

#gem 'rack', '~> 3.0' # allows 3.x, but not 4.0
gem 'rack', '~> 2.0' # allows 3.x, but not 4.0
#gem 'rack', '~> 3.0' # allows 3.x, but not 4.0
#gem 'rackup'

gem 'puma'

gem 'mcp'

